[package]
name = "magi-service-contract"
version = "0.1.0"
description = "Shared Magi service configuration and launch contracts"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
