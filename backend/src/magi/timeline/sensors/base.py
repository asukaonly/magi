"""Timeline sensor exports — legacy base removed in Phase 3.

All sensor plugins should inherit directly from ``SensorBase`` (L9).
``TimelineSensorBase`` has been removed; import ``SensorBase`` from
``magi.awareness.sensor_base`` instead.
"""
